# Codex 项目说明

## 项目背景

- 项目：2026 届全国大学生数学建模竞赛三人协作仓库。
- 比赛时间：2026-09-10 18:00 至 2026-09-13 20:00（Asia/Shanghai）。
- 当前用户主要负责编程、数据处理、建模实现、结果验证与图表输出。
- 优先保证结果可复现、文件不混乱、跨 Windows/macOS 可运行。

## 必须遵守

- 所有数学公式使用 LaTeX：行内公式用 `$...$`，独立公式用 `$$...$$`，保证不乱码。
- 开始工作前阅读 `README.md`、`FILE_NAMING_CONVENTION.md` 和 `CONTRIBUTING.md`。
- 文件名遵守 `FILE_NAMING_CONVENTION.md`，不使用“最终版”“最新版”等含糊名称。
- 原始数据只放在 `data/raw/`，禁止覆盖或原地修改。
- 代码使用项目内相对路径，不写某台电脑的绝对路径。
- 正式逻辑放在 `src/`，Notebook 只用于探索和展示。
- 图、表和数值结果分别写入 `outputs/figures/`、`outputs/tables/`、`outputs/results/`。
- 随机算法必须固定随机种子；关键模型参数、软件与依赖版本必须记录。
- 不提交密钥、Token、账号信息、虚拟环境、缓存或无必要的大型中间文件。
- 修改后运行与风险相称的检查，并明确报告未验证的部分。

## 协作原则

- 修改前先检查 `git status`，保留并避开他人的未提交改动。
- 一项任务对应一个短生命周期分支，分支格式见 `CONTRIBUTING.md`。
- 尽量避免多人同时编辑同一个 Notebook 或同一份结果文件。
- 模型或口径发生重要变化时，在 `docs/` 中补充简短决策记录。
- `main` 应始终保持可运行、可复现、可交付。

